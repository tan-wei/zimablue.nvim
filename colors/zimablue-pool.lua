-- ===================================================================
-- Zima Blue — Pool variant
-- Teal water, pale ceramic tiles — the iconic swimming pool scene.
-- ===================================================================

vim.cmd("set background=dark")
vim.g.colors_name = "zimablue-pool"

require("zimablue").setup({ _variant = "pool" })
